# Проект автоматизации тестирования доски объявлений
1. Основа для написания автотестов — фреймворк pytest.
2. Установить зависимости — pip install pytest, pip install selenium
3. Команда для запуска — pytest -v.